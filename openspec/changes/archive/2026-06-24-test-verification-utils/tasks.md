## 1. Create Unit Tests

- [x] 1.1 Create `test/test_verification_utils.py` importing `sqlglot`, the functions to test, and `pytest`.
- [x] 1.2 Implement test cases for `split_to_expressions` (nested AND and non-matching types) as per specifications.
- [x] 1.3 Implement test cases for `find_direct` (direct child and indirect children) as per specifications.

## 2. Verify

- [x] 2.1 Run the new unit tests with `.venv/bin/pytest test/test_verification_utils.py` and verify all pass.
